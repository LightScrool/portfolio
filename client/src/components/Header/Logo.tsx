import React, {FC} from 'react';
import "../../styles/Header/Logo.scss";

const Logo: FC = () => {
    return (
        <a className="Logo">
            LightScrool
        </a>
    );
};

export default Logo;